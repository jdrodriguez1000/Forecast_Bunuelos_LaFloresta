# Configuración del Proyecto: Pronóstico Buñuelos La Floresta
# Este archivo centraliza los parámetros operativos y el contrato de datos.

project_name: "Buñuelos La Floresta Forecasting"

# 📂 Rutas del Sistema (Single Source of Truth)
paths:
  raw_data: "data/01_raw/ventas_mensuales.csv"
  cleansed_data: "data/02_cleansed/"
  features_data: "data/03_features/"
  processed_data: "data/04_processed/"
  reports: "outputs/reports/"
  figures: "outputs/figures/"
  models: "outputs/models/"
  metrics: "outputs/metrics/"

# 📜 Contrato de Datos (Schema Enforcement - Phase 1)
# Aquí definimos exactamente qué columnas permitimos y de qué tipo deben ser.
data_contract:
  columns:
    fecha: "datetime"
    unidades_vendidas_mensuales: "int"
  allow_extra_columns: false  # Si viene una columna no declarada, el sistema fallará.

# 🧠 Parámetros del Modelo y Negocio
forecasting:
  horizon: 6            # Pronóstico obligatorio de 6 meses (X+1 a X+6)
  random_state: 42      # Semilla para reproducibilidad total
  test_size: 0.2        # Proporción para validación inicial

# 🔍 Configuración de Descubrimiento (Phase 1)
discovery:
  sentinel_values_numeric: [0, -1, 99, 999, 9999]
  sentinel_values_categorical: [" ", "N/A", "NA", "UNKNOWN", "EMPTY"]
  output_report_name: "phase_01_discovery_report.json"

# 🧼 Configuración de Preprocesamiento (Phase 2)
preprocessing:
  rename_columns:
    unidades_vendidas_mensuales: "unidades"
    
  sentinels:
    values: [0, -1, 999, 9999]
    action: "validate_and_nan"  # Convertir a NaN para imputación posterior

  time_series:
    frequency: "MS"             # Inicio de mes (Month Start)
    exclude_current_month: true # Regla del Mes X: Cortar datos del mes en curso
    date_column: "fecha"

  imputation:
    method: "ffill"             # Forward Fill para mantener la inercia
    fallback_method: "bfill"    # Backfill solo si el primer dato es nulo
    
  outliers:
    method: "iqr"               # Método robusto Intercuartil
    iqr_multiplier: 1.5         # Sensibilidad estándar
    flag_column: "es_atipico"   # Nombre de la nueva columna binaria
    
  output:
    filename: "ventas_preprocesadas.csv"
    report_name: "phase_02_preprocessing_report.json"

# 📊 Configuración de Análisis Exploratorio (EDA - Phase 3)
eda:
  input:
    sales_file: "ventas_preprocesadas.csv"
    macro_file: "macro_colombia.csv" # Ubicado en data/03_features/
  
  output:
    report_name: "phase_03_summary_eda.json"
    drift_report_prefix: "data_drift"
    figures_dir: "eda/"

  splitting:
    method: "dynamic"
    description: "División automática basada en el último mes completo disponible"
    validation_months: 12
    test_months: 12

  business_rules:
    structural_breaks:
      covid_period:
        start: "2020-05-01"
        end: "2021-02-28"
        label: "Impacto_COVID_Pandemia"
      
      gran_superficie_contract:
        start: "2022-06-01"
        label: "Cambio_Nivel_Retail"
    
    seasonal_events:
      feria_flores:
        month: 8
        label: "Evento_Feria_Flores"
      semana_santa:
        method: "dynamic"
        label: "Evento_Semana_Santa_Movil"
      picos_demanda:
        fin_ano: [12, 1]
        mitad_ano: [6, 7]

    exogenous_variables:
      economic: ["IPC", "TRM"]
      calendar: ["Festivos_Mes", "Fines_Semana_Mes"]

  analysis_params:
    lags_plot: 24 
    stationarity_threshold: 0.05
    decomposition_model: "additive"

# 🛠️ Configuración de Ingeniería de Características (Phase 4)
feature_engineering:
  input:
    sales_file: "ventas_preprocesadas.csv"
    macro_file: "macro_colombia.csv"
  
  output:
    filename: "ventas_features.parquet"
    report_name: "phase_04_summary_fe.json"

  # 1. Variables de Calendario
  calendar:
    country: "Colombia"
    features:
      - "festivos_conteo"
      - "fines_semana_conteo"
      - "es_semana_santa"
      - "es_feria_flores"
  
  # 2. Hitos Estructurales (Flags)
  structural_flags:
    covid_impact:
      start: "2020-05-01"
      end: "2021-02-28"
    retail_expansion:
      start: "2022-06-01"

  # 3. Variables Macroeconómicas Seleccionadas (Basadas en correlación de Fase 3)
  macro_selection:
    columns:
      - "ipc_mensual"
      - "trm_promedio"
      - "tasa_desempleo"
      - "costo_insumos_index"
      - "mes_de_prima"

